%li.media
  .media-left
    = link_to similar do
      = image_tag similar.cover.small.url.to_s, class: 'media-object'
  .media-body.desc
    = link_to similar do
      %h4.media-heading= similar.title
      %p= similar.description
