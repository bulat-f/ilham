.row
  .col-md-3
    %h3
      = "#{ @user.name} #{ @user.surname }"
    -if @user.admin?
      %span.label.label-success= t('label.admin')
    -if @user.writer?
      %span.label.label-success= t('label.writer')
  .col-md-6
  .col-md-3
    %p Здесь может еще кое-что
