%h1 All Fictions
- if current_user && current_user.admin?
  .row
    = link_to "New fiction", new_fiction_path, class: "btn btn-lg btn-primary"
- @fiction_list.each do |fictions|
  .row
    = render partial: "fiction", collection: fictions
