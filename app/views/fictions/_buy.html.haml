- unless user_signed_in?
  = link_to t('btn.buy'), new_user_session_path, class: "btn btn-primary"
- else
  - unless current_user.bought?(fiction)
    = form_for current_user.payments.build(fiction_id: fiction.id), html: { class: "auto-width" } do |f|
      .hidden= f.hidden_field :fiction_id
      = f.submit t('btn.buy'), class: "btn btn-primary auto-width"
  - else
    %p You should sign in to read fiction in this site
