- if current_user && current_user.admin?
  = link_to "Edit", edit_fiction_path(fiction), class: "label label-primary"
  = link_to "Delete", '#', mathod: :delete, class: "label label-danger", data: { confirm: "You sure?" }
