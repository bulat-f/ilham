= form_for category do |f|
  .input-group
    = f.text_field :name, class: "form-control", placeholder: Category.human_attribute_name(:name)
    %span.input-group-btn
      = f.submit t('.submit'), class: "btn btn-default"
